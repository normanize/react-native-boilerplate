import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    custom: { 
        transform: [
            { scaleX: .8 }, 
            { scaleY: .8 }
        ] 
    }
})

export default styles